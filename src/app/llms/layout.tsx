import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Best LLMs for OpenClaw 2026 (Community Ranked) | OpenClaw Rankings",
  description:
    "Compare the best large language models for OpenClaw agents in 2026. Community-ranked on pricing, quality, and real-world performance. Pricing per 1M tokens.",
};

export default function LLMsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
